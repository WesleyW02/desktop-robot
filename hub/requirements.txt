# 电脑端 Agent Hub 依赖（Phase 0 先行版）
# 安装：python -m venv .venv && .venv/Scripts/pip install -r requirements.txt

# ---- MiniMax API（OpenAI 兼容协议）----
openai>=1.30.0

# ---- MiniMax 原生端点（TTS /v1/t2a_v2、ASR /v1/asr，Token Plan 必须）----
requests>=2.31.0

# ---- 本地 ASR（Token Plan 无云端转写，用 faster-whisper 本地识别）----
faster-whisper>=1.0.0

# ---- 串口通信（Phase 1 起使用）----
pyserial>=3.5

# ---- GUI 自动化（Phase 3 起使用）----
pyautogui>=0.9.54
pygetwindow>=0.0.9

# ---- 配置解析 ----
pyyaml>=6.0

# ---- 音频（可选：语音对话用，先用 winsound 兜底）----
# sounddevice>=0.4.6
# numpy>=1.24
